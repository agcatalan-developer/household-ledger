DATABASE_URL=mysql://user:pass@localhost:3306/household_ledger
JWT_SECRET=            # 32+ chars; generate with: openssl rand -base64 48
PORT=3000
NODE_ENV=development

# Seed only — read by `npm run seed:users`, never by the server
SEED_A_EMAIL=
SEED_A_PASSWORD=
SEED_B_EMAIL=
SEED_B_PASSWORD=
